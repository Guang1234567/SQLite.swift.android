import XCTest
@testable import SQLite_swift_android

final class SQLite_swift_androidTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        // XCTAssertEqual(SQLite_swift_android().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
